type Query {
	appName: String
	allProducts: [Product!]!
	productsByAuthor(authorName: String!): [Product!]!
}

type User {
	id: ID!
	userName: String!
	fullName: String!
}

type Product {
	name: String!
	description: String!
	url: String!
	numberOfVotes: Int!
	publishedAt: String!
	author: User!
}
